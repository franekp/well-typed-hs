gen_constraints -> fix a bug where a (proper) constraint a ~ b might be prevented
from existence when a < start_var but b >= start_var

synchronize_quantifiers -> fix it so that each quantifier has it's own unique id!

exception handling guide:
ghc -prof -fprof-auto -fprof-auto-calls Main.hs -o main
./main +RTS -xc

AST processing pipeline:

InputAst
---[Syntax.prepare]--->
UAst (Maybe UPolyType)
---[TypeSystem.infer]--->
UAst UPolyType
---[Semantics.Typecheck.typecheck]--->
Poly (Ast Hi '[])
---[Semantics.Eval.polyast_to_monoast]
Mono (Ast Hi '[])
---[Semantics.Records.resolve_field_lookups]--->
Mono (Ast Lo '[])
---[Semantics.Eval.eval_monoast]--->
A Type a => a
